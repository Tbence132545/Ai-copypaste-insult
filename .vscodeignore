# Node modules and build output we don't want to publish
node_modules/
dist/

# VS Code test files
.vscode-test/
.vscode-test.mjs

# Git and version control
.git/
.gitignore

# Config files we don't need in the package
.vscode/**
.vsc-extension-quickstart.md
tsconfig.json
eslint.config.mjs

# Maps and compiled extras
**/*.map

# Ignore source TS files (compiled output is included)
src/**/*.ts
src/**/*.mjs

# Ignore local Python virtual environment
venv/
